<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fbUser %><br />
    <%= f.text_field :fbUser %>
  </div>
  <div class="field">
    <%= f.label :current_question_id %><br />
    <%= f.number_field :current_question_id %>
  </div>
  <div class="field">
    <%= f.label :isAdmin %><br />
    <%= f.check_box :isAdmin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
